/**
 * @file action-result.type.ts
 * @feature students
 * @purpose 重导出 shared ActionResult（Sprint 3 统一契约）
 */

export type {
  ActionErrorType,
  ActionResult,
  FieldErrors,
  StudentActionResult,
  StudentErrorType,
} from "@/shared/types/action-result.type"
